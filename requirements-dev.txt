pytest==9.1.1  # test runner
black==26.5.1  # formatter
isort==8.0.1   # import sorter (profile=black)
pylint==4.0.7  # linter, held at 10.00
httpx2==2.12.0 # transport for FastAPI/starlette's TestClient (httpx is deprecated there)
